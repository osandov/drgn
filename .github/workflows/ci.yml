name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      test_all_python_versions:
        description: "Run tests on all Python versions"
        type: boolean
        default: false
        required: true
      test_all_kernel_flavors:
        description: "Run tests on all kernel flavors"
        type: boolean
        default: false
        required: true
  workflow_call:
    inputs:
      test_all_python_versions:
        description: "Run tests on all Python versions"
        type: boolean
        default: false
        required: true
      test_all_kernel_flavors:
        description: "Run tests on all kernel flavors"
        type: boolean
        default: false
        required: true

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        python-version: ${{ fromJSON('["3.14", "3.13", "3.12", "3.11", "3.10", "3.9", "3.8"]') }}
        cc: [gcc, clang]
      fail-fast: false
    env:
      CC: ${{ matrix.cc }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true
      - run: python3 -m vmtest.download -c || ./rrsh.py client vps.osandov.com bash -i
